services:
  backend:
    build:
      context: ./Backend
    ports:
      - "8000:8000"
    volumes:
      - ./Backend:/app

  llm:
    build:
      context: ./LLM
    ports:
      - "8001:8001"
    environment:
      PORT: "8001"
    volumes:
      - ./LLM:/app
